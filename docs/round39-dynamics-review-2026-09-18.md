# 第39轮配置采集复核与停止排查边界

## 已核原件

输入：`C:/MyWork/deliverables/2026-09-17/artifacts/lab-readonly-dynamics-001/dynamics.json`。
SHA-256：`da0f567339d0d7e414c6b24caf818646f85eb1a9c1e057f5e3e3bde376d4d04e`。
采集源码为隔离50cff9d，execution.json退出0；complete=true、config_unchanged=true，配置哈希前后均为既有97a2c87c…f4ce208。

| 当前配置 | SDK原始值 |
|---|---|
| mass | 0.65 kg |
| cog | x=0, y=0, z=0.05（SDK标注m，参考系仍未独立确认） |
| gravity | x=0, y=0, z=-9.8（不擅自归一化或改写） |

两条读取均returned；两条单调时钟时间相等，不当作真实零耗时。该文件不包含机器人状态，不更新第37轮之后的状态结论。

## 配置支线的结论

夹爪官方自重0.46kg与配置0.65kg相差0.19kg。没有全部末端附件质量/重心资料，不能把差值直接判为错误，也不能声称已由转接件解释。重力向量有非零z分量，但这不证明其参考系和实际安装一致。

此次采集没有建立“参数错误导致反向位移”的证据。不将0.65改为0.46、不归零重心、不写重力参数；不要求实验室重复本轮或为此拆装、称重。配置支线以“实测值已取得，正确性未充分验证，保持不变”收尾，而不是“参数正常”。

## 回到真正的阻塞

第37轮已经证明：第一次stop_move仍在等待时发出了stop_sys；后续收到迟到返回，且记录到目标未相应推进的反向位移。先前motion-ID修复只解决静止停止判定，未改变这条超时升级路径。

当前没有证据保证以下任一替换安全：删除stop_sys、单纯把200ms加长、用pause_move代替、补发当前位置目标、自动恢复使能。这些都不应作为未经验证的现场快捷选项。

真正尚缺的不是更多静态回执，而是替代保护方案的工程验证：在软件停止未确认或网络反馈丢失时，如何通过独立的保护措施约束意外位移，同时避免再次依赖已出现异常的同一路软件链。此项需要现场具备机器人安全/机械能力的人员评估既有急停、制动/支撑和测试空间；不等同于必须联系厂商，但不能用“操作员在场”代替。

在这一条件明确前，主机端不能诚实地声称仅靠现有日志便能选择一个已经安全的停止策略。停止重复同样的+x动作、重复静止stop和反复归档，不再把每次离线工具通过包装成接近真机放行。

## 给实验室的简短回复

第39轮原件已核对，采集结果接受，配置保持不变。本轮不再补资料、不重采、不运动、不恢复。当前没有新的现场执行命令。后续若组织有独立保护措施的停止策略验证，会单独给出经过审查的目的、命令和边界；不从本回执自动接续VR。

本次主机工作仅新增分析文档，没有修改控制逻辑、没有推送、没有设备接触。停止故障仍未修复。
